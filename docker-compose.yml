version: "3"

volumes:
  data:

services:
  db:
    image: postgres:12
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password
      POSTGRES_DB: hoodrobin
    volumes:
      - data:/var/lib/postgresql/data
